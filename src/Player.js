import Data from './Data';

class Player {
  constructor(name) {
    this.name = name;
    this.roundScore = 0;
    this.totalScore = 0;
  }

  // quit() {

  // }

  newGame() {
    // new Game(same Players);
  }
}

export default Player;