// class Round {
//   constructor(id, question) {
        // this.id = id;
        // this.question = question;
        // correctAnswer = 0;
//   }
        //needs to happen each new round
        // getAnswer() {
          // const responses = [];
        //   if (id = id) {
        //     grab answer
        //     grab resp
        // push into responses array
        // sort by value of respondents id (highest first)
        // map.random.splice of array [1-15] to get random question id to pick question (array is reset upon new game not new round) 
      //}

        //changeQuestionSet() {
          //change inner text
        //}
        // }
// }